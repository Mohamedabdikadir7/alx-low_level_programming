#include "main.h"
/**
 * _abs - convert negative number to positive
 * @n: character to print
 * Return: -n if negative
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
